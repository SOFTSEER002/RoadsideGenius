<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.doozycod.roadsidegenius">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RoadsideGenius"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".Activities.Admin.AddServiceActivity"
            android:label="@string/titleDashboard"></activity>
        <activity
            android:name=".Activities.EditActivity"
            android:label="@string/titleDashboard" />
        <activity android:name=".Activities.Customer.DashboardCustomerActivity" />
        <activity android:name=".Activities.Customer.VerifyOTPActivity" />
        <activity
            android:name=".Activities.Driver.DriverDashboardActivity"
            android:label="@string/titleDashboard" />
        <activity
            android:name=".Activities.Admin.Navigation.DashboardAdminActivity"
            android:label="@string/titleDashboard" />
        <activity android:name=".Activities.Admin.AdminLoginActivity" />
        <activity android:name=".Activities.Customer.CustomerLoginActivity" />
        <activity android:name=".Activities.Driver.DriverLoginActvity" />
        <activity android:name=".Activities.LoginTypeActvvity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Activities.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".Activities.RequestActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".PushNotification.MyFirebaseMessagingService"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.RESPOND_VIA_MESSAGE" />
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity
            android:name=".Activities.SignupActivity"
            android:windowSoftInputMode="adjustNothing" />
        <activity android:name=".Activities.MainActivity" />
    </application>

</manifest>